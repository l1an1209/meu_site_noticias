DEBUG=true
SECRET_KEY=troque-esta-chave-em-producao
ALLOWED_HOSTS=localhost,127.0.0.1
CSRF_TRUSTED_ORIGINS=
PRODUCTION=false
DATABASE_URL=
DATABASE_SSL=true
SITE_URL=
TENANT_BASE_DOMAIN=plataforma.com.br
PLATFORM_HOSTS=localhost,127.0.0.1,testserver
TENANT_COMPAT_FALLBACK=true

EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend
EMAIL_HOST=
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
EMAIL_USE_TLS=true
DEFAULT_FROM_EMAIL=noreply@plataforma.local
NOTIFY_EMAILS=

PASSWORD_RESET_TIMEOUT=3600
SESSION_COOKIE_AGE=1209600
SERVE_MEDIA=false
SECURE_HSTS_SECONDS=31536000
SECURE_HSTS_PRELOAD=false

LOGIN_THROTTLE_LIMIT=8
LOGIN_THROTTLE_WINDOW=300
SENSITIVE_THROTTLE_LIMIT=20
SENSITIVE_THROTTLE_WINDOW=600

# Armazenamento futuro (S3 etc.) — não obrigatório agora
DEFAULT_FILE_STORAGE=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_STORAGE_BUCKET_NAME=

# Etapas comerciais
KIWIFY_WEBHOOK_SECRET=
# Token do webhook de checkout na Kiwify (validação: MD5 de order_id + token).
# Cadastre checkout_url e IDs de produto/plano em /master/planos/
# Token do webhook de checkout na Kiwify (validação MD5 de order_id + token).
# Preencha o checkout_url e os IDs de produto/plano no /master/planos/
ADSENSE_PLATFORM_CLIENT_ID=
